CREATE TABLE FIELD_HISTO (
ID INTEGER PRIMARY KEY AUTOINCREMENT,
DATE VARCHAR(30) DEFAULT CURRENT_TIMESTAMP,
ENVIRONNEMENT VARCHAR(30) NOT NULL,
TABLEE VARCHAR(30) NOT NULL,
FIELD VARCHAR(30) NOT NULL,
VALUE VARCHAR(30),
COMMMENT VARCHAR(50),
TYPE VARCHAR(30),
STATUS VARCHAR(30)
);
