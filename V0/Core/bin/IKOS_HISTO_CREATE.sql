CREATE TABLE XXXX_HISTO (
ID INTEGER PRIMARY KEY AUTOINCREMENT,
DATE_HISTO VARCHAR(30) DEFAULT CURRENT_TIMESTAMP,
DATE_UPDATE VARCHAR(30) DEFAULT CURRENT_TIMESTAMP,
TABLE_NAME VARCHAR(30) NOT NULL,
TABLE_KEY VARCHAR(30) NOT NULL,
FIELD_NAME VARCHAR(30) NOT NULL,
FIELD_VALUE VARCHAR(30),
COMMENT VARCHAR(50),
TYPE VARCHAR(30),
STATUS VARCHAR(30)
);
